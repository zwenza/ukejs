{
  "name": "ukejs-example",
  "version": "0.0.1",
  "description": "An example how to use UkeJS",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean && npm run build",
    "build": "./node_modules/.bin/babel . --out-file bundle.js --ignore node_modules",
    "clean": "./node_modules/.bin/del bundle.js"
  },
  "author": "David Joech (david.joech@gmail.com)",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-env": "^1.6.1",
    "del-cli": "^1.1.0"
  }
}
